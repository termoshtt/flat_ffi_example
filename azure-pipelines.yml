pool:
  vmImage: 'ubuntu-16.04'
steps:
  - script: |
      apt update
      apt instal -y cmake python3
    displayName: apt install
  - script: |
      cmake -Bbuild -H.
      make -C build -j
    displayName: build
